# 安全策略（Security Policy）

本项目是个人维护的早期开发项目，没有 SLA。安全问题会优先处理，但不承诺固定响应、修复或披露期限。

## 支持范围

当前只维护 `main`。仓库尚未发布可安装版本；源码测试通过不代表存在受支持的二进制发行版。

## 报告漏洞

请优先使用 GitHub **Security → Report a vulnerability** 的私有漏洞报告。请提供影响范围、最小复现、风险说明和建议缓解方式。

不要在公开 issue、PR、讨论或聊天中提交漏洞细节、凭据、QQ/OpenID、账号标识、AlecaFrame 原始快照、本机日志或真实聊天内容。如果私有漏洞报告尚未启用，只开一个不含细节的 issue，请求维护者启用私有渠道。

## 硬性安全边界

- Warframe、AlecaFrame、WFInfo、市场与账号集成保持只读；市场写操作、自动聊天、宏和游戏自动化不属于本项目。
- 凭据只能通过环境变量引用或未来明确设计的安全存储提供；普通 JSON、日志、trace、评估和文档不得保存 key。
- 个人快照只允许产生版本化白名单投影；原始字段、实例 ID、账号标识和真实玩家身份不得进入模型、trace 或评估。
- 身份与权限来自可信宿主上下文，不能由用户文本或模型自行声明。
- 测试、报告和演示只使用合成或已经脱敏的数据。

安全修复合入 `main`；若未来存在受影响的已发布版本，再发布相应补丁并清楚区分源码、CI、安装包和实机状态。
